# linux-app-icon-svg-compatibility

Corrección de compatibilidad para iconos SVG de aplicaciones Linux. El
catálogo continúa usando las raíces XDG y el proveedor de metadata existente;
este cambio sólo adapta propiedades `marker` válidas antes de entregarlas a
`usvg`.

Estado: en implementación.
