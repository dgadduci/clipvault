# Ubuntu Wayland runtime baseline

Este cambio se ejecuta y valida directamente en Ubuntu GNOME Wayland. Recupera
un baseline visible de la ventana principal antes de continuar la integración
GNOME. No modifica ni depende de `gnome-wayland-integration`.
